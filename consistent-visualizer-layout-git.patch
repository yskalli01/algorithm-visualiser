diff --git a/src/components/common/AlgorithmExplanationDrawer.tsx b/src/components/common/AlgorithmExplanationDrawer.tsx
index 0be8612..934d7c4 100644
--- a/src/components/common/AlgorithmExplanationDrawer.tsx
+++ b/src/components/common/AlgorithmExplanationDrawer.tsx
@@ -21,7 +21,7 @@ export default function AlgorithmExplanationDrawer({ open, onClose, info }: Prop
   return (
     <Drawer anchor="right" open={open} onClose={onClose}>
       <Box sx={{ width: { xs: 320, sm: 420 }, p: 3 }} role="presentation">
-        <Stack direction="row" justifyContent="space-between" alignItems="center" gap={2}>
+        <Stack sx={{ display: "flex", flexDirection: "row", justifyContent: "space-between", alignItems: "center", gap: 2 }}>
           <Typography variant="h5" sx={{ fontWeight: 800 }}>
             {info.name}
           </Typography>
@@ -63,7 +63,7 @@ export default function AlgorithmExplanationDrawer({ open, onClose, info }: Prop
         <Typography variant="h6" sx={{ fontWeight: 700, mt: 3, mb: 1 }}>
           Complexities
         </Typography>
-        <Stack direction="row" gap={1} flexWrap="wrap">
+        <Stack sx={{ display: "flex", flexDirection: "row", gap: 1, flexWrap: "wrap" }}>
           <Chip label={`Time: ${info.time}`} />
           <Chip label={`Space: ${info.space}`} />
           {info.stable && <Chip label={`Stable: ${info.stable}`} />}
diff --git a/src/components/common/AlgorithmInfoCard.tsx b/src/components/common/AlgorithmInfoCard.tsx
index 4387f21..bcfccee 100644
--- a/src/components/common/AlgorithmInfoCard.tsx
+++ b/src/components/common/AlgorithmInfoCard.tsx
@@ -19,7 +19,7 @@ export default function AlgorithmInfoCard({ info }: Props) {
           {info.description}
         </Typography>
 
-        <Stack direction="row" gap={1} flexWrap="wrap">
+        <Stack sx={{ display: "flex", flexDirection: "row", gap: 1, flexWrap: "wrap" }}>
           <Chip label={`Time: ${info.time}`} size="small" />
           <Chip label={`Space: ${info.space}`} size="small" />
           {info.stable && <Chip label={`Stable: ${info.stable}`} size="small" />}
diff --git a/src/components/layout/AppShell.tsx b/src/components/layout/AppShell.tsx
index 25a7a52..9a1a81a 100644
--- a/src/components/layout/AppShell.tsx
+++ b/src/components/layout/AppShell.tsx
@@ -35,7 +35,7 @@ type VisualizerCategory = "sorting" | "pathfinding" | "backtracking";
 
 function LoadingVisualizer({ label }: { label: string }) {
   return (
-    <Stack alignItems="center" gap={2} sx={{ py: 8 }}>
+    <Stack sx={{ display: "flex", alignItems: "center", gap: 2, py: 8 }}>
       <CircularProgress />
       <Typography color="text.secondary">{label}</Typography>
     </Stack>
@@ -49,7 +49,7 @@ export default function AppShell() {
   return (
     <Container maxWidth="lg">
       <Box sx={{ py: 4 }}>
-        <Stack direction="row" justifyContent="space-between" alignItems="center" sx={{ mb: 3 }}>
+        <Stack sx={{ display: "flex", flexDirection: "row", justifyContent: "space-between", alignItems: "center", mb: 3 }}>
           <Box sx={{ width: 48 }} />
           <Typography component="h1" variant="h3" sx={{ fontWeight: 800, textAlign: "center" }}>
             Realtime Algorithm Visualizer
diff --git a/src/components/visualizers/backtracking/BacktrackingVisualizer.tsx b/src/components/visualizers/backtracking/BacktrackingVisualizer.tsx
index 272fc30..6812e25 100644
--- a/src/components/visualizers/backtracking/BacktrackingVisualizer.tsx
+++ b/src/components/visualizers/backtracking/BacktrackingVisualizer.tsx
@@ -277,12 +277,16 @@ export default function BacktrackingVisualizer() {
 
   return (
     <Box>
-      <Typography variant="h5" sx={{ fontWeight: 700, textAlign: "center", mb: 2 }}>
+      <Typography variant="h5" sx={{ fontWeight: 700, textAlign: "center", mb: 1 }}>
         Backtracking Visualizer
       </Typography>
 
+      <Typography component="p" variant="body1" color="text.secondary" sx={{ textAlign: "center", mb: 3 }}>
+        Explore recursive decision making with N-Queens and Maze Solver animations.
+      </Typography>
+
       <Paper sx={{ p: 3, borderRadius: 4, mb: 3 }}>
-        <Box sx={{ display: "flex", gap: 2, flexWrap: "wrap", mb: 3 }}>
+        <Box sx={{ display: "flex", gap: 2, flexWrap: "wrap", alignItems: "center", mb: 3 }}>
           <FormControl sx={{ minWidth: 220 }}>
             <InputLabel>Algorithm</InputLabel>
 
@@ -338,7 +342,7 @@ export default function BacktrackingVisualizer() {
           </Button>
         </Box>
 
-        <Typography>Board Size: {size}</Typography>
+        <Typography gutterBottom>Board Size: {size}</Typography>
         <Slider
           value={size}
           min={4}
@@ -356,7 +360,7 @@ export default function BacktrackingVisualizer() {
           }}
         />
 
-        <Typography sx={{ mt: 2 }}>Speed: {speed}ms</Typography>
+        <Typography gutterBottom sx={{ mt: 2 }}>Speed: {speed}ms</Typography>
         <Slider
           value={speed}
           min={20}
@@ -365,8 +369,18 @@ export default function BacktrackingVisualizer() {
           disabled={isRunning && !isPaused}
           onChange={(_, value) => setSpeed(value as number)}
         />
+      </Paper>
 
-        <Stack direction="row" gap={1} flexWrap="wrap" sx={{ mt: 2 }}>
+      <Paper sx={{ p: 3, borderRadius: 4, overflowX: "auto" }}>
+        {algorithm === "nqueens" ? (
+          <NQueensBoard board={board} currentStep={currentStep} />
+        ) : (
+          <MazeBoard maze={maze} onCellClick={handleMazeCellClick} />
+        )}
+      </Paper>
+
+      <Paper elevation={1} sx={{ mt: 3, p: 3, borderRadius: 4 }}>
+        <Stack sx={{ display: "flex", flexDirection: "row", gap: 1, flexWrap: "wrap" }}>
           <Chip label={`Step: ${stepIndex}/${totalSteps}`} />
           <Chip label={algorithm === "nqueens" ? `Checks: ${checks}` : `Visited: ${checks}`} />
           <Chip label={`Backtracks: ${backtracks}`} />
@@ -379,12 +393,6 @@ export default function BacktrackingVisualizer() {
         <AlgorithmInfoCard info={backtrackingInfo[algorithm]} />
       </Paper>
 
-      {algorithm === "nqueens" ? (
-        <NQueensBoard board={board} currentStep={currentStep} />
-      ) : (
-        <MazeBoard maze={maze} onCellClick={handleMazeCellClick} />
-      )}
-
       <AlgorithmExplanationDrawer open={drawerOpen} onClose={() => setDrawerOpen(false)} info={backtrackingInfo[algorithm]} />
     </Box>
   );
diff --git a/src/components/visualizers/pathfinding/PathfindingVisualizer.tsx b/src/components/visualizers/pathfinding/PathfindingVisualizer.tsx
index fec63a2..845a38e 100644
--- a/src/components/visualizers/pathfinding/PathfindingVisualizer.tsx
+++ b/src/components/visualizers/pathfinding/PathfindingVisualizer.tsx
@@ -290,12 +290,16 @@ export default function PathfindingVisualizer() {
 
   return (
     <Box>
-      <Typography variant="h5" sx={{ fontWeight: 700, textAlign: "center", mb: 2 }}>
+      <Typography variant="h5" sx={{ fontWeight: 700, textAlign: "center", mb: 1 }}>
         Pathfinding Visualizer
       </Typography>
 
+      <Typography component="p" variant="body1" color="text.secondary" sx={{ textAlign: "center", mb: 3 }}>
+        Draw walls, drag the start/end nodes, then watch the selected pathfinding algorithm run step by step.
+      </Typography>
+
       <Paper sx={{ p: 3, borderRadius: 4, mb: 3 }}>
-        <Stack direction="row" gap={2} flexWrap="wrap" alignItems="center">
+        <Stack sx={{ display: "flex", flexDirection: "row", gap: 2, flexWrap: "wrap", alignItems: "center", mb: 3 }}>
           <FormControl sx={{ minWidth: 220 }}>
             <InputLabel>Algorithm</InputLabel>
             <Select value={algorithm} label="Algorithm" disabled={isRunning} onChange={(e) => setAlgorithm(e.target.value as PathfindingAlgorithm)}>
@@ -315,21 +319,28 @@ export default function PathfindingVisualizer() {
           <Button variant="outlined" color="error" onClick={clearGrid}>Clear Grid</Button>
         </Stack>
 
-        <Typography sx={{ mt: 3 }}>Speed: {speed}ms</Typography>
+        <Typography gutterBottom>Speed: {speed}ms</Typography>
         <Slider value={speed} min={5} max={120} step={5} disabled={isRunning && !isPaused} onChange={(_, value) => setSpeed(value as number)} />
 
-        <Stack direction={{ xs: "column", md: "row" }} gap={3} sx={{ mt: 2 }}>
+        <Stack sx={{ display: "flex", flexDirection: { xs: "column", md: "row" }, gap: 3, mt: 2 }}>
           <Box sx={{ flex: 1 }}>
-            <Typography>Rows: {rowCount}</Typography>
+            <Typography gutterBottom>Rows: {rowCount}</Typography>
             <Slider value={rowCount} min={8} max={25} step={1} disabled={isRunning && !isPaused} onChange={(_, value) => resizeGrid(value as number, colCount)} />
           </Box>
           <Box sx={{ flex: 1 }}>
-            <Typography>Columns: {colCount}</Typography>
+            <Typography gutterBottom>Columns: {colCount}</Typography>
             <Slider value={colCount} min={12} max={50} step={1} disabled={isRunning && !isPaused} onChange={(_, value) => resizeGrid(rowCount, value as number)} />
           </Box>
         </Stack>
+      </Paper>
 
-        <Stack direction="row" gap={1} flexWrap="wrap" sx={{ mt: 2 }}>
+      <Paper sx={{ p: 3, borderRadius: 4, overflowX: "auto" }}>
+        <Legend items={[{ label: "Start", color: "#2e7d32" }, { label: "End", color: "#d32f2f" }, { label: "Wall", color: "#212121" }, { label: "Visited", color: "#0288d1" }, { label: "Path", color: "#ed6c02" }]} />
+        <PathfindingGrid grid={grid} onNodeMouseDown={handleNodeMouseDown} onNodeMouseEnter={handleNodeMouseEnter} onMouseUp={handleMouseUp} />
+      </Paper>
+
+      <Paper elevation={1} sx={{ mt: 3, p: 3, borderRadius: 4 }}>
+        <Stack sx={{ display: "flex", flexDirection: "row", gap: 1, flexWrap: "wrap" }}>
           <Chip label={`Visited: ${visitedCount}`} />
           <Chip label={`Path nodes: ${pathLength}`} />
           <Chip label={`Frame: ${frameIndex}/${frames.length}`} />
@@ -339,11 +350,6 @@ export default function PathfindingVisualizer() {
         <AlgorithmInfoCard info={pathfindingInfo[algorithm]} />
       </Paper>
 
-      <Paper sx={{ p: 3, borderRadius: 4, overflowX: "auto" }}>
-        <Legend items={[{ label: "Start", color: "#2e7d32" }, { label: "End", color: "#d32f2f" }, { label: "Wall", color: "#212121" }, { label: "Visited", color: "#0288d1" }, { label: "Path", color: "#ed6c02" }]} />
-        <PathfindingGrid grid={grid} onNodeMouseDown={handleNodeMouseDown} onNodeMouseEnter={handleNodeMouseEnter} onMouseUp={handleMouseUp} />
-      </Paper>
-
       <AlgorithmExplanationDrawer open={drawerOpen} onClose={() => setDrawerOpen(false)} info={pathfindingInfo[algorithm]} />
     </Box>
   );
diff --git a/src/components/visualizers/sorting/SortingVisualizer.tsx b/src/components/visualizers/sorting/SortingVisualizer.tsx
index 56bbe94..c5d5a1e 100644
--- a/src/components/visualizers/sorting/SortingVisualizer.tsx
+++ b/src/components/visualizers/sorting/SortingVisualizer.tsx
@@ -189,7 +189,26 @@ export default function SortingVisualizer() {
           Visualize sorting algorithms step by step with animated bars.
         </Typography>
 
-        
+
+        <SortingControls
+          speed={speed}
+          isRunning={isRunning}
+          onSpeedChange={setSpeed}
+          onStart={startSorting}
+          onReset={resetArray}
+          onGenerate={generateNewArray}
+          algorithm={algorithm}
+          onAlgorithmChange={setAlgorithm}
+          onPauseResume={togglePause}
+          onStepForward={stepForward}
+          onStepBackward={stepBackward}
+          onOpenExplanation={() => setDrawerOpen(true)}
+          canStep={steps.length > 0 || !isRunning}
+          isPaused={isPaused}
+          arrayLength={arrayLength}
+          onArrayLengthChange={setArrayLength}
+        />
+
         <Paper elevation={3} sx={{ mt: 3, p: 3, borderRadius: 4 }}>
         <Legend
           items={[
@@ -208,7 +227,7 @@ export default function SortingVisualizer() {
         </Paper>
 
         <Paper elevation={1} sx={{ mt: 3, p: 3, borderRadius: 4 }}>
-          <Stack direction="row" gap={1} flexWrap="wrap">
+          <Stack sx={{ display: "flex", flexDirection: "row", gap: 1, flexWrap: "wrap" }}>
             <Chip label={`Step: ${stepIndex}/${totalSteps}`} />
             <Chip label={`Comparisons: ${comparisons}`} />
             <Chip label={`Writes/Swaps: ${swaps}`} />
diff --git a/src/lib/algorithmInfo.ts b/src/lib/algorithmInfo.ts
index b9b8f1b..136de4d 100644
--- a/src/lib/algorithmInfo.ts
+++ b/src/lib/algorithmInfo.ts
@@ -125,13 +125,13 @@ export const backtrackingInfo = {
     optimal: "Finds a valid solution, not an optimization problem",
     description: "Places queens row by row and backtracks whenever a queen attacks another queen.",
     howItWorks: "The solver tries to place one queen per row. If a queen conflicts with an existing queen, it tries the next column. If no column works, it removes the previous queen and backtracks.",
-    pseudocode: "solve(row):
+    pseudocode: `solve(row):
   if row == n: solution found
   for each col:
     if safe(row, col):
       place queen
       solve(row + 1)
-      remove queen",
+      remove queen`,
     useCases: ["Teaching recursion", "Constraint satisfaction", "Backtracking demos"],
   },
   maze: {
@@ -142,13 +142,13 @@ export const backtrackingInfo = {
     optimal: "No, DFS does not guarantee shortest path",
     description: "Uses depth-first search with backtracking to find any valid route from start to end.",
     howItWorks: "The solver visits a cell, recursively tries each direction, and marks dead ends by backtracking when a route cannot reach the end.",
-    pseudocode: "solve(cell):
+    pseudocode: `solve(cell):
   if cell is wall or visited: return false
   mark visited
   if cell is end: return true
   for each direction:
     if solve(next cell): mark path and return true
-  backtrack and return false",
+  backtrack and return false`,
     useCases: ["Maze solving", "Teaching DFS", "Showing recursive backtracking"],
   },
 } as const;
